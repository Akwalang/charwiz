[package]
name = "logger"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Colored console logger with optional markup and aliases."
repository = "https://example.com/your-repo"

[features]
logger = []

default = [
  "logger",
]
